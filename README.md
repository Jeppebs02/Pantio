# Introduction 
TODO: Write this.

# Other stuff
In prod replace .env.dev with a new .env.prod file :)

You can add your own AGENTS.md and CLAUDE.md in the repo root.

Also .env.dev is added to gitignore (should also be placed in repo root), find the one I use here:

https://fileshare.navisystems.dpdns.org/s/Dev-Env/dev-env-pantio.zip

password is "pantio"

---

To start the compose do `docker compose --env-file .env.dev up -d`
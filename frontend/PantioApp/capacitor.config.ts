import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pantio.app',
  appName: 'PantioApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
